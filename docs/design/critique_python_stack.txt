key: v2:16379f9de10a8fe65ed11081a58bf8a996b2d2e0ab22359a6c2173a728c88eb6
issues: 19

### 1. [high] D10 / §1 pyproject: torch는 extras(cpu/cu124)에만 두고 `[[tool.uv.index]] pytorch-cpu = https://download.pytorch.org/whl/cpu (explicit)` 에서 받는다. T1 검증: `uv sync --ex
PROBLEM: 샌드박스에서 download.pytorch.org 가 차단되어 있어(프록시 게이트웨이가 CONNECT에 403 응답) 이 pyproject는 `uv lock` 자체가 불가능하다(cpu fork 메타데이터를 그 index에서 가져와야 함). 실제로 앞선 워크플로우가 이 지점에서 실패했고(23:35:18~23:42:43 사이 download.pytorch.org:443 connect_rejected 8회), 이미 커밋된 cf89d7f 의 pyproject는 이를 우회해 PyPI torch(`torch>=2.5` → 2.14.0+cu130, 5.9 GB venv)를 base deps에 넣었다. 이 상태에서 `torch.cuda.is_available()`은 False로 CPU 실행은 되지만 `torch.version.cuda == '13.0'` 이므로 계획의 `test_cpu_extra_has_no_cuda`(`torch.version.cuda is None`)는 반드시 실패한다. 또한 cu124 index는 torch 2.6.x에서 끊겨 있어 계획대로 하면 샌드박스(2.14 cpu)와 H100(2.6 cu124)이 서로 다른 torch 버전으로 갈라져 `weights_only` 기본값 등 동작 차이가 생긴다.
FIX: (1) 기존 커밋의 정책을 채택: torch는 PyPI 단일 소스, `torch==2.14.*`로 핀, `cpu/cu124` extras·`[tool.uv] conflicts`·`tool.uv.sources` 삭제, `[tool.uv] environments = ["sys_platform == 'linux'"]` 추가로 lock 축소. (2) ADR-005/README H100 체크리스트에 "`nvidia-smi` 상단 CUDA Version ≥ 13.0(드라이버 ≥ 580) 확인; 미달이면 주석 처리된 `pytorch-cu128`/`cu126` index 블록을 활성화하고 H100에서 `uv lock` 후 커밋(lock_hash 변경 기록)"을 명시. (3) `tests/test_env_smoke.py::test_cpu_extra_has_no_cuda` 를 `test_torch_importable_and_cuda_flag_consistent`(assert `collect_env_snapshot().cuda_available == torch.cuda.is_available()`, `torch.version.cuda`에 대한 절대값 단언 없음)로 교체. (4) T1 검증 명령을 `uv sync --extra dev && uv lock --check && uv run python -c "import torch;print(torch.cud

### 2. [high] D5 + §7 cross validator: protocol_hash에 `target_adaptation`·`protocol_id` 포함, `adaptation.enabled == protocol.target_adaptation.enabled` 강제. §6/§10: adapt.py는 `
PROBLEM: e02(source-only, `syn_a_to_b_v1`, adaptation.enabled=false)와 e03(`syn_a_to_b_bf_adapt_v1`, enabled=true)은 정의상 절대 같은 protocol_hash를 가질 수 없고, cross validator 때문에 source-only run을 adaptation protocol 아래에서 돌릴 수도 없다. 따라서 `tracker.find_baseline(e02, hash(bf_adapt_v1))`은 항상 None → e03은 항상 `inconclusive`(note 'no baseline') → §14.3/§41-5가 요구하는 DA 전후 per-PAI APCER 자동 비교(security regression gate)가 하네스 전체에서 단 한 번도 자동 실행되지 않는다. 리포트도 매번 `--justify`가 필요해 §15 '같은 hash → 직접 비교'가 Baseline A vs B에 구조적으로 적용 불가. 실데이터 E02→E03/E04/E05 모두 동일한 문제.
FIX: Phase 0에서는 (a)안 채택: cross validator를 `adaptation.enabled ⇒ protocol.target_adaptation.enabled` (단방향)으로 완화하고, `adaptation.method == none`은 '해당 protocol의 control arm'으로 허용(adaptation set은 동일하게 select·test에서 제외되어 test set이 완전히 같음). e02 실험 파일을 `protocol: syn_a_to_b_bf_adapt_v1` + `adaptation: none` 으로 바꾸어 e02/e03이 같은 protocol_hash를 공유하게 하고 `syn_a_to_b_v1`은 e01 전용으로 둔다. 추가로 `SecurityGateResult.baseline_run_id`를 기록하고, `tests/integration/test_adapt_smoke.py::test_e03_gate_finds_e02_baseline_without_justify` 를 추가해 verdict가 `inconclusive(no baseline)`가 아님을 단언. (대안 (b): `eval_hash`(source_datasets, target_dataset, target_test, attack_types, threshold, acer_policy, security_gate, source_classes, allow_

### 3. [high] D3 gate: `GIT_OK(sha not None and (not dirty or allow_dirty_tree))` 와 `SMOKE_OK(registry.latest_success_smoke(science_hash, git_sha))` 를 동시에 요구. 파일 트리: `experim
PROBLEM: smoke run 자체가 git 추적 파일(registry.jsonl, adaptation set jsonl)을 변경해 트리를 dirty로 만든다. 이후 full run은 GIT_OK(clean) 때문에 커밋을 요구하는데, 커밋하면 git_sha가 바뀌어 SMOKE_OK(smoke row의 git_sha == 현재 sha)가 실패한다. 즉 `allow_dirty_tree: false`인 정상 경로에서는 full run 게이트를 영원히 통과할 수 없다(교착). 계획의 'smoke→freeze→full' 순서도 freeze가 추적 파일을 쓰므로 같은 문제를 만든다. 또한 `tests/integration/test_train_smoke.py`가 실제 `scripts/train.py`를 띄우면 repo의 registry/mlruns.db/outputs가 오염되고 트리가 dirty해진다.
FIX: (1) `experiments/registry.jsonl`과 `data/manifests/adaptation/*`는 gitignore(기존 커밋 .gitignore가 이미 그렇게 함 — 유지)하고 README에 'registry는 MLflow의 파생 인덱스, 재생성 가능'을 명시. (2) `utils/git.py::git_state()`는 `git status --porcelain --untracked-files=no` 기준으로 dirty를 계산하되 gitignore된 경로는 자연히 제외되므로 추가 예외 불필요; `untracked_count`는 정보용으로만. (3) 절차를 `validate_spec --freeze → git commit → smoke(train.py +exp=X) → 실험 파일 mode:full 편집+커밋 → smoke 재실행(같은 sha) → full` 이 아니라, 단순화해 '실험 파일에 mode:full/allow:true까지 다 적고 commit → smoke는 `execution.mode` override 없이 `PAD_FORCE_SMOKE=1`… ' 은 env 우회라 금지이므로, 대신 `train.py --smoke` 전용 CLI 플래그(스펙을 바꾸지 않고 mode를 smoke로 강등만 허용, 승격은 불가)를 두어 같은 커밋(sha)에서 smoke→full을 연속 실행하게 한다. gate 규칙: `SMOK

### 4. [high] D2: `science_hash = sha256(canonical_json(spec.model_dump(exclude={"execution","tracking"})))` — smoke spec과 full spec이 같은 값; SPEC_FROZEN/SMOKE_OK/baseline 탐색 단
PROBLEM: exclude 집합이 너무 좁다. (a) `adaptation.source_run_id`/`source_checkpoint`는 adapt 실행 시 CLI로 주입되고(`adaptation.source_run_id=<id>`), smoke 소스 run과 full 소스 run이 다르며 frozen spec에는 null이다 → 모든 full adapt run에서 SPEC_FROZEN과 SMOKE_OK가 구조적으로 실패. (b) `experiment.title/hypothesis/parent_experiment_id`, `protocol.description/status/change_note`(protocol_hash에서는 제외했는데 science_hash에는 포함되어 두 hash 정책이 모순), `evaluation.baseline_experiment_id/latency`, `training.device/num_workers`(H100에서 num_workers를 올리면 hash 변경) 같은 비과학 필드가 포함되어 문구 수정·워커 수 변경만으로 smoke↔full 연결이 끊긴다. (c) `model_dump()`가 python 모드라 protocol_hash(`mode="json"`)와 직렬화 규칙이 다르다.
FIX: `config/schema.py`에 `SCIENCE_EXCLUDE = {"execution": True, "tracking": True, "adaptation": {"source_run_id", "source_checkpoint"}, "experiment": {"title", "hypothesis", "parent_experiment_id"}, "protocol": set(HASH_EXCLUDED), "evaluation": {"baseline_experiment_id", "latency", "measure_latency"}, "training": {"device", "num_workers"}}` 를 정의하고 `science_hash = sha256_text(canonical_json(spec.model_dump(mode="json", exclude=SCIENCE_EXCLUDE)))`. 제외된 값은 MLflow tag(`checkpoint_source`, `parent_experiment_id`, `baseline_experiment_id`)와 registry 컬럼으로 기록. SPEC_FROZEN은 science_hash 비교(이미 그렇게 정의)이므로 frozen 파일에 source_run_id가 null이어도 통과. 테스트: `test_science_hash_ignores_source_run_id_device_and_text_fie

### 5. [medium] §2 Hydra config 예시: `hydra:\n  run:   {dir: outputs/${experiment.id}/${now:%Y%m%d-%H%M%S}}`, `sweep: {dir: multirun/${experiment.id}/..., subdir: "${hydra.job.n
PROBLEM: YAML flow mapping 안에 따옴표 없는 `${...}`는 `{`가 중첩 flow mapping 시작으로 파싱되어 PyYAML/OmegaConf가 `ParserError: expected ',' or '}', but got '{'`를 낸다. 계획에 적힌 config.yaml과 data/synthetic.yaml은 그대로 쓰면 compose 단계에서 전부 실패한다(=`test_compose_default_config_validates`부터 깨짐).
FIX: 보간이 들어가는 값은 block style로 쓰거나 문자열을 따옴표로 감싼다: `hydra:\n  run:\n    dir: outputs/${experiment.id}/${now:%Y%m%d-%H%M%S}`, `loader:\n  batch_size: ${training.batch_size}\n  num_workers: ${training.num_workers}\n  pin_memory: false`. `coding-style.md`에 'configs/**.yaml에서 flow mapping + `${}` 금지' 규칙 추가, `tests/unit/test_compose.py::test_every_config_yaml_parses` (`yaml.safe_load` 전수) 추가. 참고로 `+exp=<name>` + `# @package _global_` + `defaults: - override /model: video_baseline` + `_self_` 선행 조합은 hydra-core 1.3.7에서 의도대로 동작함을 확인했고(`exp=` without `+`는 ConfigCompositionException), OmegaConf는 `1e-3`도 float로 읽으므로 그 부분은 문제 없음.

### 6. [medium] D3/§7: smoke가 silently full이 되지 않도록 CLI `execution.*` override는 hook deny + in-process `FLAG_NOT_FROM_CLI`(overrides에 'execution.' 접두 토큰 없음)로 막는다. `SmokeLimits(
PROBLEM: (1) `SmokeLimits` 필드에 상한이 없어 실험 파일(또는 `+exp` 파일)에 `smoke: {max_batches: 100000, max_epochs: 50}`를 적으면 gate가 전혀 관여하지 않는 `mode: smoke`로 사실상 full training이 가능하다. (2) `FLAG_NOT_FROM_CLI`가 'execution.' 접두만 보면 Hydra의 `+execution.mode=full`, `++execution.mode=full`, `~execution.smoke` 같은 토큰을 놓친다(`++execution.mode=full`, `execution.smoke.max_batches=99999`가 compose에서 그대로 반영됨을 확인). (3) `--config-dir/-cd`, `--config-name/-cn`, `--config-path/-cp`로 다른 exp 트리를 주입하는 경로가 hook·gate 어디에도 명시되지 않음.
FIX: `class SmokeLimits(BaseModel): max_batches: int = Field(2, ge=1, le=10); max_epochs: int = Field(1, ge=1, le=1); max_eval_batches: int = Field(2, ge=1, le=10); latency_repetitions: int = Field(1, ge=1, le=3)`. `gate.py::_touches_execution(tok)`: `tok.lstrip('+~').strip('\'"').split('=')[0] in {'execution'} or startswith('execution.')`; hook EXP-01도 같은 정규화 함수 사용(`_common.py`에 공유). gate hook에 EXP-07: 세그먼트에 `-cd/--config-dir/-cn/--config-name/-cp/--config-path` 토큰이 있으면 deny(테스트 fixture 디렉터리 `tests/fixtures/configs`만 allow). Trainer는 `limits = SmokeLimits(**spec.execution.smoke) if spec.execution.mode == 'smoke' else None` 한 곳에서만 결정하고, `RunStatus.smoke_ok`는 `limits is not None`일 때만 부여. 테스트: `tes

### 7. [medium] §4 `ProtocolValidation.adaptation_set_hash: str | None` 를 `validate_protocol()`이 반환; D6 `materialize_adaptation_set(...)`는 `data/manifests/adaptation/<protocol_
PROBLEM: validator가 adaptation_set_hash를 내려면 선택을 수행해야 하는데, 계획대로면 materialize(파일 쓰기)가 validator 경로에 얽혀 PreToolUse/PostToolUse hook 안에서 `data/manifests/**`에 파일을 쓰게 된다(보호 대상 디렉터리를 hook이 스스로 변경; §24.4 취지 위반, 읽기 전용이어야 할 검증이 부작용을 가짐). 또 hook이 임의 경로(`--path`)의 draft protocol을 검증할 때 쓰레기 adaptation 파일이 생긴다.
FIX: `protocols/adaptation_set.py`를 둘로 분리: `select_adaptation_set(protocol: ProtocolSpec, target: Manifest) -> AdaptationSelection{sample_ids, subject_ids, adaptation_set_hash}` (순수 함수: 정렬 → `random.Random(selection_seed).sample`) 와 `materialize_adaptation_set(selection, out_dir) -> Path` (쓰기). `validate_protocol`은 select만 호출해 hash·leakage 검사(`ADAPT_TEST_SAMPLE_OVERLAP` 등)를 수행하고, 파일 쓰기는 `scripts/train.py|adapt.py`의 run 시작 시(및 MLflow artifact `adaptation_set.jsonl`)에서만. 테스트 `tests/protocol/test_adaptation_set.py::test_validate_protocol_has_no_filesystem_side_effects` (tmp manifests_dir 스냅샷 비교).

### 8. [medium] §4 protocol YAML: `security_gate: {abs_tolerance: 0.01, rel_tolerance: 0.0, min_attack_samples_per_pai: 20}`; §3 SyntheticAdapter: n_subjects=20, split (0.6,0.2
PROBLEM: synthetic_b test split은 4 subject × (print 1, replay_phone 1, replay_tablet 1) = PAI당 공격 4개뿐이라 `min_attack_samples_per_pai: 20`에 항상 미달 → 모든 PaiDelta가 `insufficient_support` → `run_security_gate`는 합성 데이터에서 항상 `inconclusive`. `security_regression` 판정 경로(§14.3의 핵심)가 end-to-end에서 한 번도 실행되지 않고, `test_full_finetune_bf_only_produces_regression_check`도 verdict를 검증할 수 없다. 또 verdict 우선순위(일부 PAI만 지지 부족일 때)가 정의되지 않았다.
FIX: 합성 protocol 두 개는 `security_gate.min_attack_samples_per_pai: 4`(합성 전용 hash이므로 실데이터와 무관)로 두고 ADR-005에 사유 기록; 또는 `SyntheticAdapter(n_subjects=30, clips_per_subject=6, split_ratio=(0.4,0.2,0.4))`로 test PAI당 12개 확보. verdict 규칙을 코드와 docstring에 고정: `if any(d.regressed and not d.insufficient_support): security_regression; elif any(d.insufficient_support): inconclusive; else: pass` (bpcer_improved는 verdict에 영향 없음). 통합 테스트에 verdict가 `inconclusive`일 경우 note에 'insufficient support'가 없어야 한다는 단언 추가(`test_synthetic_gate_has_sufficient_pai_support`).

### 9. [medium] §9 `training/checkpoint.save_checkpoint(path, model, spec, threshold: ThresholdPolicy | None, protocol_hash, strategy_state) / load_checkpoint(path) -> Checkpoi
PROBLEM: torch ≥ 2.6은 `torch.load` 기본이 `weights_only=True`라 pickle된 Pydantic 모델/StrEnum이 포함된 체크포인트는 `UnpicklingError: Weights only load failed`로 실패한다(설치된 torch 2.14.0에서 재현). `weights_only=False`로 우회하면 신뢰되지 않은 체크포인트 로딩 경고/보안 문제가 생기고 H100 쪽 torch 버전에 따라 동작이 갈린다.
FIX: 체크포인트를 `{"state_dict": model.state_dict(), "meta": {"spec": spec.model_dump(mode="json"), "threshold": threshold.model_dump(mode="json") if threshold else None, "protocol_hash": ..., "science_hash": ..., "git_sha": ..., "strategy_state": <json-able dict>, "format_version": 1}}` 로 저장(모든 값은 tensor·기본 타입만), `load_checkpoint`는 `torch.load(path, map_location="cpu", weights_only=True)` 후 `ExperimentSpec.model_validate(meta["spec"])`, `ThresholdPolicy.model_validate(...)`로 복원. 테스트 `tests/unit/test_checkpoint.py::test_checkpoint_roundtrip_weights_only_true`.

### 10. [medium] §9 `FrameEncoderTemporalTransformer`: shared encoder per frame → (B,T,D) + pos-emb → `nn.TransformerEncoder` → pool → head; `TinyCNN`: conv→BN→ReLU→pool ×3.
PROBLEM: (1) `nn.TransformerEncoderLayer`는 기본 `batch_first=False`라 (B,T,D)를 (T,B,D)로 해석하는데 출력 shape이 (B,T,D)로 동일해 `test_video_baseline_shape`는 통과하면서 attention이 배치 축을 섞는 silent bug가 된다(확인). (2) BatchNorm은 train 모드에서 B·T == 1인 배치를 만나면 'Expected more than 1 value per channel' 예외 — frame_baseline(T=1)에서 마지막 배치 크기 1이면 발생(adaptation set 24개/batch 8은 괜찮지만 fixture나 CLI batch_size 변경 시 터짐). (3) 결정성 테스트(allclose 1e-6)를 위해 dropout·nested tensor fast path를 통제해야 한다.
FIX: `nn.TransformerEncoderLayer(d_model=embed_dim, nhead=heads, dim_feedforward=2*embed_dim, dropout=dropout, batch_first=True, norm_first=True)`, `nn.TransformerEncoder(layer, num_layers=depth, enable_nested_tensor=False)`; config `net.dropout: 0.0`. 학습 DataLoader는 `drop_last=True`(smoke에서도), 또는 TinyCNN 정규화를 `GroupNorm(4, w)`로 교체(HeadOnly의 'BN eval' 규칙은 GroupNorm이면 불필요). 테스트 추가: `test_video_baseline_batch_independence` (배치의 다른 샘플을 바꿔도 i번째 출력이 allclose) — batch_first 누락을 잡는다; `test_frame_baseline_batch_size_one_train_mode`.

### 11. [medium] §7/§10 `paths.py`: REPO_ROOT, MANIFESTS_DIR, SPECS_DIR, REGISTRY_PATH 상수; `tests/fixtures/specs/full_cpu_ok.yaml`로 `tests/integration/test_train_full_cpu.py`가 g
PROBLEM: (1) 통합 테스트는 `scripts/train.py`를 subprocess로 띄우므로 conftest의 monkeypatch가 닿지 않는다 → 실제 repo의 `experiments/registry.jsonl`, `mlruns.db`, `outputs/`, `experiments/specs/`에 쓰게 되고 결과가 테스트 간·개발 run과 뒤섞인다(SMOKE_OK가 테스트 row를 근거로 통과하는 오염 포함). (2) `tests/fixtures/specs/full_cpu_ok.yaml`은 Hydra search path에 없어 `+exp=full_cpu_ok`로 compose되지 않는다; `--config-dir`를 쓰려면 파일이 `<dir>/exp/full_cpu_ok.yaml` 구조여야 한다. (3) `hydra.job.chdir: false` 상태에서 `manifests_dir: data/manifests`, `sqlite:///mlruns.db`, `outputs/`가 모두 cwd 상대라 repo root 밖에서 실행하면 깨진다.
FIX: `paths.py`를 env-override 가능하게: `REPO_ROOT = Path(os.environ.get("PAD_REPO_ROOT", Path(__file__).resolve().parents[2]))`, `REGISTRY_PATH = Path(os.environ.get("PAD_REGISTRY_PATH", REPO_ROOT/"experiments/registry.jsonl"))`, `SPECS_DIR`, `OUTPUT_ROOT`(`PAD_OUTPUT_ROOT`) 동일; tracker 기본 URI `f"sqlite:///{REPO_ROOT}/mlruns.db"`, `mlflow.create_experiment(..., artifact_location=str(REPO_ROOT/"mlruns_artifacts"))`; `hydra.run.dir: ${oc.env:PAD_OUTPUT_ROOT,outputs}/${experiment.id}/${now:...}`. fixture는 `tests/fixtures/configs/exp/full_cpu_ok.yaml`, `full_needs_gpu.yaml`로 옮기고 테스트는 `uv run python scripts/train.py --config-dir tests/fixtures/configs +exp=full_cpu_ok` 를 tmp env(`PAD_REGISTRY_PATH`, `PAD

### 12. [low] §7 테스트 `test_spec_from_section16_example`(fixture: §16 YAML에서 `family: g2v2former`→`video_baseline`, dataset id 소문자만 바꿈); `ExperimentSpec(extra="forbid")` with 
PROBLEM: §16 예시는 `data.source/data.target`(DataSpec에 없음 → extra=forbid로 거부), `protocol` 블록 없음(필수), `experiment.research_question` 없음(필수), `execution.mode` 없음(기본 smoke라 OK)이라 '두 군데만 바꾼 fixture'로는 검증이 불가능하다. 테스트 이름이 약속하는 '§16 예시가 그대로 통과한다'는 주장은 거짓이 된다.
FIX: 두 가지 중 택일: (a) `DataSpec`에 `source: list[str] | None = None`, `target: list[str] | None = None`을 허용하고 cross validator에서 `protocol.source_datasets/target_dataset`와 일치하지 않으면 `DATA_PROTOCOL_MISMATCH`로 거부(§16 형태를 문서 호환으로 수용); `ExperimentMeta.research_question` 기본값 `"harness"`; `protocol`은 §16처럼 생략 시 `configs/protocol` 기본 그룹에서 채워지므로 fixture는 Hydra compose 경유로 검증. (b) 테스트를 `test_section16_derived_fixture_validates`로 개명하고 fixture 상단 주석에 §16 대비 변경 목록(data 블록 교체, protocol 추가, research_question 추가)을 적고 ADR-005에 매핑 표 기록. 권장: (b) + research_question 기본값.

### 13. [low] §4 `protocol_hash = sha256(canonical_json(p.model_dump(mode="json", exclude=HASH_EXCLUDED)))`, `canonical_json = json.dumps(obj, sort_keys=True, separators=(","
PROBLEM: (1) `source_datasets`, `target_dataset`, `attack_types`, `source_classes`는 의미상 집합인데 순서를 보존하므로 `[OULU_NPU, CASIA_FASD]`와 `[CASIA_FASD, OULU_NPU]`가 다른 protocol로 취급되어(확인) 동일 실험이 '비교 불가'가 된다. (2) hash가 기본값을 포함하므로 `ProtocolSpec`에 필드를 추가하거나 기본값(예: `min_attack_samples_per_pai` 20→30)을 바꾸면 커밋된 모든 protocol의 hash가 조용히 바뀌어 과거 run과의 비교가 끊긴다; golden 테스트 1건(fixture 1개)만으로는 커밋된 protocol 파일들의 drift를 잡지 못한다. (3) `json.dumps`는 기본으로 NaN/Infinity를 통과시켜(확인) 잘못된 float가 hash에 섞일 수 있다.
FIX: `ProtocolSpec`에 `@field_validator("source_datasets","target_dataset","attack_types","source_classes")`로 `sorted(set(lower()))` 정규화(중복 거부는 별도 error `DUPLICATE_DATASET`), ADR-004에 '집합 필드는 정렬 후 hash' 명시. `canonical_json(..., allow_nan=False)`. `ProtocolSpec.schema_version: Literal[1] = 1`을 hash에 포함해 스키마 변경을 명시적으로 만들고, `tests/fixtures/protocol_hashes.json`에 `configs/protocol/*.yaml` 전부의 hash를 pin하는 `test_committed_protocol_hashes_unchanged`(변경 시 `--justify`성 커밋 메시지와 fixture 갱신을 요구) 추가.

### 14. [low] §5 toy vectors 표와 §8 테스트: `test_auc_partial` 0.625(벡터 미명시), security gate toy `replay_phone.delta == 0.13`, `test_gate_within_tolerance_passes`(after replay .02
PROBLEM: toy 값 재계산 결과: BPCER 0.25, APCER {print 0.25, replay_phone 0.6}, max 0.6, pooled 4/9, ACER(max_pai) 0.425, HTER 0.34722, EER separable (tau 0.6, eer 0.0), overlap (tau 0.6, eer 0.5), 경계 [0.5]→BPCER 1.0, AUC ties 0.5 — 모두 정확하며 sklearn 1.9.1 `roc_curve(drop_intermediate=False)` + `thr==inf` 제외 규칙으로 재현됨. 다만 (a) `test_auc_partial`은 입력 벡터가 없어 구현자가 임의 값을 고르면 0.625가 안 나올 수 있고, (b) delta 비교를 `==`로 쓰면 부동소수점에 취약하다(0.15-0.02==0.13은 우연히 참이지만 0.025-0.02=0.005000000000000001처럼 tolerance 경계 비교가 흔들린다), (c) EER 값 정의((fpr+fnr)/2 vs fpr)와 `bpcer_at_apcer`의 선택 규칙(target APCER 이하인 최대 tau)이 미정의.
FIX: `test_auc_partial`: bona `[0.1, 0.5, 0.6, 0.7]`, attack `[0.2, 0.3, 0.8, 0.9]` → 0.625(검증됨). 모든 실수 기대값은 `pytest.approx(x, abs=1e-12)`로 비교. `security_gate.regressed = delta > max(abs_tol, rel_tol*apcer_before) + 1e-12`가 아니라 `delta - tol > 1e-9`처럼 경계 여유를 두고 docstring에 '경계값(정확히 tol)은 pass'를 명시. `eer_threshold` docstring: 'tau = 유한 threshold 중 |FPR-FNR| 최소의 첫 index, eer = (FPR+FNR)/2'; `bpcer_at_apcer(target)`: 'APCER(tau) <= target 을 만족하는 tau 중 최대값(=BPCER 최소)'. 두 규칙 모두 toy 테스트로 고정.

### 15. [low] §8 `test_cpu_extra_has_no_cuda`(`torch.version.cuda is None`), `test_no_gpu_in_sandbox_is_recorded`; H100 이관 절차에 `make test` 포함.
PROBLEM: 이 테스트들은 샌드박스의 하드웨어 사실을 절대값으로 단언하므로 H100(cuda_available=True)에서 `make test`가 반드시 실패하며, PyPI torch(cu130)를 쓰는 현재 환경에서는 샌드박스에서도 실패한다. '같은 테스트 스위트를 두 환경에서 그대로 돌린다'는 계획의 전제와 모순.
FIX: 환경 스냅샷 테스트는 일관성만 검증: `assert snap.cuda_available == torch.cuda.is_available()`, `assert snap.gpu_count == len(snap.gpu_names)`, `assert (snap.driver_version is None) == (snap.gpu_count == 0)`, `assert snap.lock_hash == sha256_file(REPO_ROOT/'uv.lock')`. 샌드박스 전용 단언이 필요하면 `@pytest.mark.skipif(torch.cuda.is_available(), reason='GPU host')`로 분리. `env_snapshot.torch_build`('cpu'|'cu130'…, `torch.version.cuda` 기반)를 새 필드로 기록해 lock_hash와 함께 MLflow tag `torch_build`로 남긴다.

### 16. [low] §8 MLflow tracker: `tracking_uri null → sqlite:///mlruns.db`, `find_baseline(experiment_id, protocol_hash, include_smoke)`, `log_metrics ... apcer_<pai>` 기록; ho
PROBLEM: MLflow 3.16.1(설치본) sqlite 백엔드는 정상 동작(초기 마이그레이션 ~4 s, `search_runs(filter_string="tags.experiment_id = '...' and tags.status = 'smoke_ok'")` OK)하지만 (a) `import mlflow` 시 stderr에 INFO 'agent hint' 로그가 찍혀 hook/CLI의 `--json` 출력이나 stderr 파싱을 오염시킬 수 있고, (b) NaN metric이 조용히 저장되므로 공격 0개 PAI를 `apcer_<pai>=nan`으로 넣으면 리포트 집계(mean/std)가 NaN으로 전파된다, (c) `mlflow.search_runs`는 `experiment_names`를 주지 않으면 현재 experiment만 보므로 baseline이 다른 `mlflow_experiment`에 있으면 못 찾는다, (d) sqlite 백엔드의 기본 artifact root는 cwd 상대 `./mlruns`.
FIX: `MlflowTracker.__init__`에서 `os.environ.setdefault("MLFLOW_DISABLE_AGENT_HINT", "1")` 후 import; `log_metrics`는 `math.isfinite` 아닌 값은 기록하지 않고 `EvalResult.notes`에 'PAI x: no support' 기록; `find_baseline`은 `mlflow.search_runs(search_all_experiments=True, filter_string=f"tags.experiment_id = '{exp}' and tags.protocol_hash = '{h}'" + ("" if include_smoke else " and tags.status = 'success'"), order_by=["attributes.start_time DESC"])`; experiment 생성 시 `artifact_location=str(REPO_ROOT/'mlruns_artifacts')`. 테스트 `test_log_metrics_skips_nonfinite`, `test_find_baseline_across_experiments`.

### 17. [medium] Context/T0/T1: 'Repo github.com/Imvely/harness is EMPTY except LICENSE', T0에서 `CLAUDE.md` 원문 cp(`wc -l` = 2586), T1에서 pyproject/uv.lock 신규 작성, `requires-python 
PROBLEM: 작업 디렉터리에는 이미 커밋 cf89d7f('chore: repository scaffold, research contract, and pyproject')가 있다: pyproject(torch PyPI base dep, `<3.13`, `dvc` 별도 extra, `.claude/hooks` ruff 제외), uv.lock(189 packages), `.venv`(5.9 GB), `.gitignore`(registry·adaptation set 무시, `experiments/approvals/` 디렉터리 — 계획이 기각한 Approval 개념의 흔적), `docs/RESEARCH_CONTRACT.md`(업로드 원문과 바이트 동일, 2585줄), 그리고 root `CLAUDE.md` 없음, `.claude/{agents,hooks,rules}` 비어 있음. 계획은 이 상태를 모른 채 '빈 repo'를 전제하므로 T0/T1이 기존 파일과 충돌하고, `wc -l CLAUDE.md = 2586` 단언은 실제 파일(2585줄, diff는 동일)에서 실패한다.
FIX: T0 앞에 T-1 '기존 스캐폴드 조정' 단계 추가: (1) `docs/RESEARCH_CONTRACT.md`를 root `CLAUDE.md`로 `git mv`(또는 `CLAUDE.md`가 `@docs/RESEARCH_CONTRACT.md` import 한 줄만 갖게 — 사용자 결정), (2) `experiments/approvals/` 제거(D3 결정과 불일치), (3) pyproject는 기존 PyPI-torch 정책 유지 + `torch==2.14.*` 핀 + `[tool.uv] environments` 추가, `requires-python`은 `>=3.11,<3.13` 유지 여부를 ADR-005에 기록, (4) `.gitignore`의 registry/adaptation 무시는 유지(issue 'GIT_OK 교착' 해결책과 일치). `test_claude_md_verbatim`은 줄 수가 아니라 `sha256_file(CLAUDE.md) == '05625405...'`(커밋 메시지에 기록된 값)로 비교.

### 18. [low] D15/EXP-04: hook이 `uv run --no-sync python scripts/validate_spec.py --for-launch --json` 호출(45 s, torch 미import); §7 `experiments/validator.py`는 torch 미import, 
PROBLEM: (1) validator 경로가 가벼우려면 `pad_research/tracking/__init__.py`, `experiments/__init__.py`, `config/__init__.py`도 비어 있어야 하고 `gate.py`가 `EnvSnapshot`·`Registry` 타입만 가져와야 하는데 계획은 `pad_research/__init__.py`만 언급한다; `tracking/__init__.py`가 `mlflow_tracker`를 re-export하면 import당 mlflow 1.2 s + torch 1.7 s(5.9 GB CUDA 라이브러리 로딩)가 hook마다 붙고 stderr에 mlflow hint가 섞인다. (2) `post_edit_check.py`의 `uv run`(--no-sync 없음)은 매 편집마다 lock 검사·동기화를 수행하며 `.venv`를 hook이 변경할 수 있다(예: lock이 바뀐 직후 편집 시 패키지 설치/제거).
FIX: 모든 `__init__.py`를 빈 파일로 고정하고 `tests/unit/test_spec_validator.py::test_validator_import_graph_is_light`에서 `subprocess.run([sys.executable, '-c', 'import sys, pad_research.experiments.validator, pad_research.experiments.gate; assert not ({"torch","mlflow","sklearn","matplotlib"} & set(sys.modules))'])`로 단언; 하네스의 모든 hook 호출은 `uv run --no-sync`, Makefile은 `UV_NO_SYNC=1`을 export하고 명시적 `make setup`에서만 `uv sync`. `[dependency-groups] dev = [...]` + `[tool.uv] default-groups = ["dev"]`로 pytest/ruff가 항상 존재하도록 해 `uv run pytest`가 sync 없이 동작.

### 19. [low] §3 `SyntheticAdapter.build_manifest`: subject당 bona_fide 3 / print 1 / replay_phone 1 / replay_tablet 1, `generate_clip(sample_id, ...)` 시드 = sha256(sample_id);
PROBLEM: (1) `ManifestRecord`의 model_validator가 `bona_fide ⇔ pai none`을 로드 시점에 거부하므로 validator의 `LABEL_PAI_INCONSISTENT`는 도달 불가 코드이고, 테스트 fixture(`bad_*.jsonl`)는 `load_manifest`에서 pydantic ValidationError로 먼저 죽는다(또 fixture는 `.meta.json`+manifest_hash 없이는 `verify_manifest_hash`도 통과 못 함). (2) sample_id에 dataset_id가 없으면 A/B가 같은 sample_id·같은 generator 시드를 갖고 adaptation 파일명·MLflow 기록에서 모호해진다. (3) target 도메인 B(밝기 -0.15, gain 1.3, noise 0.05, color cast)로 2 epoch(≈18 step, lr 1e-3, tiny CNN)만에 AUC>0.6을 요구하는 slow 테스트는 flaky할 가능성이 높다.
FIX: (1) 잘못된 manifest fixture 테스트는 `ManifestRecord.model_construct(...)`로 만든 in-memory 레코드를 `validate_records(records)`에 직접 넣는 형태로 바꾸고, 파일 기반 fixture는 `write_manifest`로 테스트 setup에서 생성(메타·hash 자동). `LABEL_PAI_INCONSISTENT`는 '방어선'으로 유지하되 docstring에 정상 경로에서는 pydantic이 먼저 잡는다고 명시. (2) `sample_id = f"{dataset_id}_s{subject:03d}_c{clip:02d}_{pai}"`. (3) 학습 sanity 단언은 source dev(in-domain) AUC ≥ 0.8 로 두고 target test AUC는 기록만; 또는 fixture `epochs: 5`, `learning_rate: 3.0e-3`. 결정성 단언(`allclose(atol=1e-6)`)은 `torch.set_num_threads(1)`, `OMP_NUM_THREADS=1`, `dropout=0`, `drop_last=True` 조건에서만.
